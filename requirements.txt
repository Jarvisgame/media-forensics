# 依赖清单（版本锁定：2026-09-23，Python 3.11）
# 系统依赖（非 pip）：exiftool —— Debian/Ubuntu: sudo apt install libimage-exiftool-perl

numpy==2.4.6
opencv-python-headless==5.0.0.93
pillow==12.3.0
piexif==1.1.3

# 后续阶段按需启用：
# gradio        # W8 薄界面
# torch         # W5 AIGC 基线推理（CPU 版即可）
