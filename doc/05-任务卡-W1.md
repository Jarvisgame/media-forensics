# 05 · 任务卡 W1（第 1 周：骨架 + 证据链 + 元数据 v0）

> 计划投入：约 9h（1~2h/天）
> **周验收**：`analyze <样例图>` 跑通 → `cases/` 生成案件记录 + `report.json` + 哈希链校验通过

## T1.1 环境与仓库（1.5h）

- [x] **1a（0.5h）** 创建 conda 环境 `media-forensics`（`conda create -n media-forensics python=3.11`）并在环境内安装 `requirements.txt` 依赖；核对 `exiftool -ver` 可用（Debian/Ubuntu：`sudo apt install libimage-exiftool-perl`）
- [x] **1b（0.5h）** 按 `doc/01-系统架构.md` §7 核对目录结构；冒烟：`python -c "import cv2, PIL, piexif, numpy"`
- [ ] **1c（0.5h）** git 初始化与首次提交 —— **由你自行执行**（本仓库约定：不在自动化流程中操作 git）

## T1.2 数据模型 + 案件库 + 哈希链（2.5h）

- [ ] **2a（1h）** `src/forensics/models.py`：`MediaRef` / `AnalysisResult` / `Finding` 数据类
- [ ] **2b（1h）** `src/forensics/store.py`：SQLite 建表（cases/media/analyses/findings/chain，DDL 见 `doc/01` §6）+ 写入接口 `append_case()` / `append_analysis()`
- [ ] **2c（0.5h）** `scripts/verify_chain.py`：哈希链校验脚本（顺序重算 `record_hash` 并比对）

## T1.3 插件协议 + CLI（2.5h）

- [ ] **3a（1h）** `src/forensics/analyzers/base.py`：`Analyzer` 协议 + 分析器注册表
- [ ] **3b（1h）** `src/forensics/pipeline.py`：运行器（质量门占位 + 按注册顺序执行分析器 + 汇总）
- [ ] **3c（0.5h）** `src/forensics/cli.py`：`analyze` 命令（生成案件目录 + `report.json` + 落库）

## T1.4 元数据模块 v0（2.5h）

- [ ] **4a（1h）** `analyzers/metadata.py`：ExifTool JSON 解析 + 基础字段提取
- [ ] **4b（1h）** 规则表 v0：软件字段 / 时间线矛盾 / 尺寸异常 三类规则
- [ ] **4c（0.5h）** 接入管线，用 `data/samples/` 样例图跑通端到端

## 收工记录

| 日期 | 完成任务卡 | 耗时 | 卡点 | 下一步 |
|---|---|---|---|---|
|  |  |  |  |  |

## 检查点提醒

- 若 W1 超时 >30%：优先保证 **2b / 3a** 完整（骨架与留痕是硬线，其他可微调）
- 下周预告：W2 信号痕迹（ELA/噪声残差/双压缩）任务卡将在 W1 收工后生成
